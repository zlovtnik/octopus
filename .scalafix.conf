rules = [
  OrganizeImports
]

# This Scala 3.3.8/sbt-scalafix toolchain rejects Scalafix's RemoveUnused rule.
# Unused imports remain enforced by -Wunused:all and -Werror.
OrganizeImports {
  groupedImports = Merge
  removeUnused = false
  targetDialect = Scala3
}
